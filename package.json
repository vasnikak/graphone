{
  "name": "graphone",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts"
  ]
}
